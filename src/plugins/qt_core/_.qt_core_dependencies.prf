
QT *= sql
QT *= xml

load(../../libs/extensionsystem/extensionsystem.prf)
load(../../libs/utils/utils.prf)

DIRS = \
    .. \
    actionmanager \
    dialogs \
    interfaces \
    . \

for(dir, DIRS) {
    INCLUDEPATH *= $$PWD/$$dir
    DEPENDPATH *= $$PWD/$$dir
}

